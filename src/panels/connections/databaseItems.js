import React from 'react';

function DatabaseItems(props) {

  const generateItems = (databases) => {
    return databases.map(db => {
      return 
    })
  }
  
  return (

  );
}

export default DatabaseItems;